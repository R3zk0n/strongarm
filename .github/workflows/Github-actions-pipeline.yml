name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Code linting
    runs-on: ubuntu-latest
    # Skip lint on manual trigger if you only care about tests there
    if: github.event_name != 'workflow_dispatch'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'   # <- was 3.7

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements*.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install lint deps
        shell: bash
        env:
          PIP_DISABLE_PIP_VERSION_CHECK: "1"
        run: |
          pip install -U 'pip<23.2' wheel setuptools 'pip-tools<7.0.0'
          pip-sync requirements.txt requirements-dev.txt

      - name: Project install (invoke)
        shell: bash
        run: invoke install

      - name: Autoformat + lint
        shell: bash
        run: invoke autoformat-lint

  test_pip_sync:
    name: Test (via pip-sync / invoke)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'   # <- was 3.7

      - name: Install system packages
        shell: bash
        run: |
          sudo apt-get update -y
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends \
            libcapstone4 \
            libcapstone-dev \
            sqlite3 \
            libsqlite3-dev

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements*.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install Python deps (pip-sync)
        shell: bash
        env:
          PIP_DISABLE_PIP_VERSION_CHECK: "1"
        run: |
          pip install -U 'pip<23.2' wheel setuptools 'pip-tools<7.0.0'
          pip-sync requirements.txt requirements-dev.txt

      - name: Project install (invoke)
        shell: bash
        run: invoke install

      - name: Run tests (invoke)
        shell: bash
        run: invoke test

  test_setuptools:
    name: Test (via setuptools)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'   # <- was 3.7

      - name: Install system packages
        shell: bash
        run: |
          sudo apt-get update -y
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends \
            libcapstone4 \
            libcapstone-dev \
            sqlite3 \
            libsqlite3-dev

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip-setuptools-${{ hashFiles('setup.py') }}
          restore-keys: |
            ${{ runner.os }}-pip-setuptools-

      - name: Install Python tools
        shell: bash
        env:
          PIP_DISABLE_PIP_VERSION_CHECK: "1"
        run: |
          pip install invoke mypy flake8 pytest pytest-xdist

      - name: Version info
        shell: bash
        run: |
          python --version
          pip --version

      - name: Install package (setuptools path)
        shell: bash
        run: pip install .

      - name: Run tests (invoke)
        shell: bash
        run: invoke test
